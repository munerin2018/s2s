# セキュリティ

## 報告

脆弱性を見つけた場合は、公開 Issue ではなく
[GitHub の Security Advisory](../../security/advisories/new) から報告してください。

P2P アプリなので、**攻撃者はあなたのピアと同じネットワークにいる普通の参加者**です。
特に次のようなものを探しています。

- ピアから送られたデータで、クラッシュ・ハング・過大なメモリ消費が起きるもの
- 署名の検証を回避できるもの、他人になりすませるもの
- ログの外にあるファイルを読ませたり書かせたりできるもの
- 片方の実装だけが受理／拒否するイベントを作れるもの（ネットワーク分断）
- 他人の投稿を隠したり、届かなくさせたりできるもの

## 設計上そうなっているもの

次は脆弱性ではなく、仕様です。報告不要です。

- **投稿はすべて公開です。** 暗号化も DM もありません。
- **誰と接続しているかは相手に分かります。** メタデータは秘匿していません。
- **鍵を失うとアカウントは戻りません。** 再発行する主体が存在しません。
- **削除は墓標です。** すでに複製されたイベントを物理的に消すことはできません。
- **スパムと Sybil 攻撃への耐性は弱いです。** 現状ブロックのみで、鍵は無料で作れます。

詳しくは [docs/LIMITS.md](docs/LIMITS.md) を見てください。

## これまでに直したもの

0.1.0 の公開前にレビューを通し、次を修正しました。いずれも正常系のテストでは見つからない種類のものです。

| | |
|---|---|
| blob プロトコルのパストラバーサル | `{"blob":"&../identity.json"}` でアカウントの秘密鍵を送信できた |
| リモートクラッシュ | 4 バイトのゴシップ 1 通、非 ASCII の blob id でピアが停止した |
| リモートハング | 不正な have ベクタ 1 通でイベントループが停止した |
| 検閲 | 未着イベントの id を先に指定すると誰でも他人の投稿を隠せた |
| ネットワーク分断 | 実装間で正準 JSON が一致しない入力を作れた |
| 増幅 | イベントに任意データを貼り付けて全ピアに転送させられた |
| フォーク検出のすり抜け | 並行書き込みで同じ `seq` に 2 件入った |

回帰テストは `packages/core/test/hostile.test.js` と Rust 側の `mod tests` にあります。
**機能を足すときは、ここにも 1 件足してください。**
